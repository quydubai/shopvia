{
  "name": "clone99-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute clone99-db --file=./schema.sql",
    "db:seed": "wrangler d1 execute clone99-db --file=./seed.sql"
  },
  "dependencies": {
    "hono": "^4.3.0",
    "jose": "^5.3.0"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
